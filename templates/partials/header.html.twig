<h1 class="text-center">{% block titre %}FastBurger.com{% endblock %}</h1>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="{{path('home')}}">FastBurger</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02"
    aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor02">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{path('home')}}">Accueil</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 1})}}">Menus</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 2})}}">Burgers</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 3})}}">Snack</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 4})}}">Salades</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 5})}}">Boissons</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="{{path('menu', {id : 6})}}">Desserts</a>
      </li>


      {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item active">
        <a class="nav-link" href="{{path('admin_menu')}}">Administration</a>
      </li>
      {% endif %}
    </ul>
    <ul class="navbar-nav ml-auto">
      {% if app.user %}
      <li class="nav-item dropdown mr-4">
        <a href="" class="nav-link dropdown-toggle" id="DropdownLink" data-toggle="dropdown">
            <img src="{{app.user.picture}}" class="avatar avatar-mini" alt="Avatar de {{app.user.username | capitalize}}"> {{app.user.username | capitalize}}
        </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="DropdownLink">
          <a class="dropdown-item" href="{{path('account_index')}}">Mon Profil</a>
          <a  class="dropdown-item" href="{{path('panier')}}">Mon Panier</a>
          <div class="dropdown-divider"></div>
          <a  class="dropdown-item" href="{{path('logout')}}">Déconnexion</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('login')}}">Connexion</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('inscription')}}">Inscription</a>
        </li>
      </div>
    </div>
    {% endif %}
    </ul>
  </div>
</nav>

