{% extends 'base.html.twig' %}

{% block title %}FastBurger.com{% endblock %}

{% block titre %}FastBurger.com{% endblock %}

{% block body %}

<main class="container">
    <h1>{{categorie.nom}} </h1>
    <section class="row">
        {% for menu in categorie.menu %}
        <div class=" col-md-4 mb-3">
            <div class="card">
                <img src="{{vich_uploader_asset(menu,'imageFile')}}" alt="{{menu.nom}}">
                <div class="card-body">
                    <p>{{menu.prix}}€</p>
                    <h3>{{menu.nom}}</h3>
                    <p class="card-text">
                        {{menu.description}}
                    </p>
                </div>
                {% if not app.user %}
                <p class="mt-5">Pour commander:</p>
                <a href="{{path('login')}}" class="btn btn-primary mb-2">Se connecter</a>
                <a href="{{path('inscription')}}" class="btn btn-primary mb-2">S'inscrire</a>
                {% else %}
                <a href="{{path('panier_add', {'id' : menu.id})}}" class="btn btn-danger mb-2">Commander</a>
                {% endif %}
            </div>
        </div>
        {% endfor %}
    </section>
</main>
{% endblock %}